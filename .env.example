# Example environment variables for the optional proxy or local testing.
# Copy this file to `.env` and replace placeholders with real values.
# Do NOT commit your real `.env` to source control.

# Your Gemini / Vertex API key (if calling directly from a server proxy)
GEMINI_KEY=

# Optional custom Vertex endpoint or proxy URL. For a proxy set this to e.g. https://my-proxy.example.com/api/ai
GEMINI_ENDPOINT=

# If you deploy a small proxy, use a short token to protect it.
PROXY_SECRET=

# Local proxy URL for testing
PROXY_URL=http://localhost:3000/api/ai
